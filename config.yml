# Hardstop config (YAML, flat keys)
# Computer will shutdown during quiet hours and keep shutting down if turned back on

start_time: "21:00"              # 9 PM - quiet hours start (shutdown enforced)
end_time: "08:00"                # 8 AM - quiet hours end
lock_interval_seconds: 300       # 5 minutes - how often to check/shutdown
